<% if @agent == @post.agent %>
  <% form_tag("/post/#{@post.id}", class: 'delete', method: 'delete') do %>
    <input type="submit" value="&#9249;">
  <% end %>
<% end %>
<%= partial('post/post', {locals: {post: @post, can_vote: @agent.can_vote?(@post)}}) %>
